common errors:

- You don't need "\H" and "\/" to print "H" and "/". That would lead to warning of compilers.
Simply "H" and "/" is sufficient.

- If you want to start a new line, you can use "\n" or endl. Using both will skip a new line.
